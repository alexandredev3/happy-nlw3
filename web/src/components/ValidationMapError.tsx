import React from 'react';

import { Container } from '../styles/components/validation-map-error';

const ValidationMapError = () => {
  return (
    <Container>
      <p>Selecione um local no mapa.</p>
    </Container>
  );
}

export default ValidationMapError;